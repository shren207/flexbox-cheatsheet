export default function FlexWrap({ value }: { value: string }): JSX.Element {
  // value => nowrap | wrap | wrap-reverse
  // tailwindcss => flex-no-wrap | flex-wrap | flex-wrap-reverse
  return <div>flexwrap</div>;
}
